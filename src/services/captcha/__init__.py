"""Captcha submodule — pure helpers extracted from browser_captcha_personal.

Runtime error classification + proxy helpers live here (no live browser needed).
The nodriver runtime + tab-pool state machine remain in browser_captcha_personal.
"""
